# Books CSV Builder + Recommender
# Python 3.7+

# Core dependencies (minimal for now)
# Add more as needed for OCR, advanced matching, etc.

# API dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Testing
pytest>=7.4.0

# OCR/physical shelf processing (optional - only needed for shelf photo ingestion)
pillow>=9.0.0
pytesseract>=0.3.10

# For future: advanced fuzzy matching
# fuzzywuzzy>=0.18.0
# python-Levenshtein>=0.12.0

